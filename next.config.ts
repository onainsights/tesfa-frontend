

/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      '127.0.0.1',        
      'localhost',        
      'tesfa-53b1c4b2cf65.herokuapp.com', 
    ],
  },
};

export default nextConfig;